// TODO : make a module in the util

/*function isArray(v) {
	return (v.indexOf("lst") >= 0) ? true: false;
}

function isExistArray(props, ps, v) {
	return (props[pc].indexOf(v) < 0) ? true: false;
}*/